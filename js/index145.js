const user = {
    username : 'batman',
    password: 'Alfred1960ROCKS!',
  };
 if (user.username = batman && password = Alfred1960ROCKS!)
{ console.log(`Welcome back batman!!`);}
else{console.log ()}

capitalize.function (string) {
    let newstring = string.charAt(0).toUpperCase() + string.slice(1);
    return newstring;
}
  
